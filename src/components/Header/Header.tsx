import { Button } from "../Button"

export const Header = () => {
  return (
    <div className="flex">
      <Button>
        Donate
      </Button>

      <Button>
        Volunteer
      </Button>
    </div>
  )
}
